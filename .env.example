# Database configuration (FastAPI will auto-create sqlite DB in the current directory)
DATABASE_URL=sqlite+aiosqlite:///./nasheed.db

# Server settings
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO

# Application configurations
SCORE_THRESHOLD=0
RATE_LIMIT_PER_MINUTE=60
